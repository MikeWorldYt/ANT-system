import { AddHyphenPipe } from './add-hyphen.pipe';

describe('AddHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new AddHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});
